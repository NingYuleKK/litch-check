# Litch's Check 项目交接文档

**最后更新时间:** 2026年3月2日

**文档作者:** Manus AI

## 1. 项目概览

- **项目名称:** Litch's Check
- **项目简介:** 一款个人目标反馈与习惯追踪工具，旨在通过游戏化的方式帮助用户保持动力，完成每日设定的目标。
- **核心理念:** 结合积极心理学与趣味性设计，将日常任务转化为富有成就感的体验。

## 2. 技术栈

| 分类 | 技术 | 版本/说明 |
| :--- | :--- | :--- |
| **前端框架** | React | `19` |
| **UI 库** | Tailwind CSS | `4` |
| **语言** | TypeScript | 最新稳定版 |
| **构建工具** | Vite | 最新稳定版 |
| **后端** | Express + tRPC | `4` / `11` |
| **数据库** | MySQL (TiDB) | 通过 Manus webdev 平台集成 |
| **动画** | Framer Motion | 最新稳定版 |
| **截图导出** | modern-screenshot | `4.6.x` |

## 3. 功能列表

### 3.1 每日打卡系统

- **五类固定任务:**
  - 包含"学英语"、"读书"、"AI/AIGC"、"功课与正念"五个核心任务（原"锻炼"任务已升级为独立模块）。
  - 每个任务都有专属的颜色和手绘风格图标，便于视觉区分。

- **每日打卡:**
  - 用户可以为每个完成的任务勾选"完成"。
  - 支持为每次打卡添加简短的文字记录或心得。

- **成就系统:**
  - **星星成就:** 当天完成 ≥3 个任务时，获得"猫竖大拇指"图标。
  - **大圆满成就:** 当天完成所有 5 个任务时，获得"猫抱莲花"图标。

- **司马黑（胖黑猫吉祥物）:**
  - 根据用户的打卡状态，吉祥物会切换四种不同的形态（Normal / Encourage / Perfect / Lazy）。
  - 带有贱萌风格的对话气泡，根据完成状态和连胜天数动态生成。

- **连胜天数追踪:**
  - 自动记录用户连续打卡的天数。
  - 在达到特定的里程碑（如 3 天、7 天、30 天）时，会发送祝贺消息。

### 3.2 数据可视化

- **月日历视图:** 在日历上清晰展示每日的成就状态（无标记、星星或莲花）。
- **每周完成率:** 以柱状图的形式展示最近一周内每日任务的完成比例。
- **月度统计:** 汇总展示当月的总打卡天数、各项成就获得次数等数据。

### 3.3 日记与回顾

- 提供一个独立的每日日记/Log 区域，供用户记录详细感想。
- 用户可以编辑和删除过去的打卡记录和日记。

### 3.4 周报结算系统 (v2.0 新增)

- **自动生成周报:**
  - 基于每周一到周日的打卡数据，自动计算并生成结算单。
  - 无需额外的数据库表，直接从 `checkins` 表聚合查询。

- **结算单内容:**
  - **星星天数:** 当天完成 ≥3 个任务的天数（满分 7）。
  - **大圆满天数:** 当天完成所有 5 个任务的天数（满分 7）。
  - **5 个分类各自的完成情况:** 每个分类本周完成次数 / 7 天。
  - **司马黑评语:** 根据本周表现动态生成的贱萌风格一句话评语。

- **猫评语规则:**

| 表现等级 | 条件 | 评语风格 |
| :--- | :--- | :--- |
| 全勤周 (perfect) | 7 天都 ≥3 | "这周你比我还勤快，不科学。" |
| 大圆满多 (lotusMany) | ≥3 天全 5 | "莲花都快被你摘完了，佛祖要找你谈话。" |
| 一般般 (decent) | 4-6 天 ≥3 | "还行吧，勉强配得上我的陪伴。" |
| 摸鱼周 (slacking) | 1-3 天 ≥3 | "这周你是不是把我忘了？" |
| 完全没打卡 (absent) | 0 天 ≥3 | "……你还活着吗？" |

- **周一弹窗提醒:**
  - 每周一打开网站时，自动弹窗提醒用户查看上周周报。
  - 使用 `localStorage` 记录已查看状态，避免重复弹窗。

- **历史周报列表:**
  - 底部导航栏新增"周报"入口，可随时查看历史周报。
  - 列表按时间倒序排列，显示每周的星星天数和大圆满天数。
  - 当前周（还没到周一）显示"待结算"状态。

- **导出图片:**
  - 使用 `modern-screenshot` 库（`domToBlob`）将结算卡片导出为 PNG 图片。
  - 2x 分辨率，保留孟菲斯风格装饰和猫图片。

### 3.5 Root 批语功能 (v2.3 新增)

- **功能描述:**
  - 用户每天打完卡后，可以将当天打卡图片发给 GPT，GPT 会写一条批语/点评。
  - 用户可以将 GPT 的批语粘贴到系统中，与当天的打卡记录关联。

- **UI 位置:**
<<<<<<< HEAD
  - 在每日结算弹窗（点击日历上某天弹出的详情弹窗）中，"今日随笔"下方显示 Root 批语区域。
  - 批语区域采用绿色系背景（`#B8E8D0`），与"今日随笔"的紫色系背景区分。
=======
  - 在每日结算弹窗（点击日历上某天弹出的详情弹窗）中，“今日随笔”下方显示 Root 批语区域。
  - 批语区域采用绿色系背景（`#B8E8D0`），与“今日随笔”的紫色系背景区分。
>>>>>>> 43392b5a69e73bcf41583f0bbb29776fdca8dce0
  - 使用 🪐 emoji 作为标识。

- **交互逻辑:**
  - 导出区域内：仅显示静态批语内容（有批语时显示，无批语时不显示）。
<<<<<<< HEAD
  - 导出区域外：提供编辑入口（无批语时显示虚线占位框，有批语时显示"编辑 Root 批语"链接）。
  - 编辑时展开 textarea，支持保存和取消。

- **导出图片:**
  - 有批语时，导出的 PNG 图片在"今日随笔"下方显示批语区域，带 🪐 标识。
=======
  - 导出区域外：提供编辑入口（无批语时显示虚线占位框，有批语时显示“编辑 Root 批语”链接）。
  - 编辑时展开 textarea，支持保存和取消。

- **导出图片:**
  - 有批语时，导出的 PNG 图片在“今日随笔”下方显示批语区域，带 🪐 标识。
>>>>>>> 43392b5a69e73bcf41583f0bbb29776fdca8dce0
  - 无批语时，导出图片与现有效果完全一致。

### 3.6 锻炼模块 (v3.0 新增)

- **功能描述:**
<<<<<<< HEAD
  - 独立的锻炼训练记录模块，用于记录每天做了什么训练。
  - 数据存储使用 `localStorage`（本地存储），不依赖云端数据库。
  - 底部导航栏新增"锻炼"入口（哑铃图标）。

- **训练记录:**
  - 按日期记录训练，支持日期前后切换。
  - 每条训练记录包含多个训练动作。
  - 每个动作支持两种记录方式：
    - **组数/次数模式:** 记录每组的次数和重量（如：第1组 12次 × 20kg）。
    - **时长模式:** 记录训练时长（分钟），适用于有氧运动。
  - 支持添加训练感想/备注。

- **添加动作:**
  - **常见动作列表:** 预置 15 个常见训练动作（俯卧撑、深蹲、平板支撑等），一键添加。
  - **自定义动作:** 支持输入自定义动作名称、描述、记录方式和图片 URL。

- **Star 标记（常用动作库）:**
  - 支持 Star 某个训练动作，标记为"长期固定训练"。
  - Star 过的动作形成"常用动作库"，可通过"常用动作"按钮一键查看和使用。
  - 从常用动作库选择动作时，自动填入默认配置。
  - 支持取消 Star 和从常用动作库中移除。

- **图片展示:**
  - 每个训练动作可关联一张图片（预留 `imageUrl` 字段）。
  - 在训练记录卡片和常用动作库中展示动作图片。
  - 无图片时显示哑铃图标占位符。
  - 默认 placeholder 图片：司马黑举杠铃。

- **导出卡片:**
  - 可将单个训练记录导出为 PNG 图片卡片。
  - 卡片规格：3:4 竖版。
  - 卡片布局：上方 75% 为动作图片，下方 25% 为动作名称 + 简短说明 + 训练数据 + 日期。
  - 孟菲斯风格装饰：圆形、菱形、三角形几何元素。
  - 底部水印："LITCH'S CHECK"。
  - 使用 `modern-screenshot` 库（`domToBlob`）导出，2x 分辨率。

- **空状态:**
  - 无训练记录时显示司马黑（lazy 状态）插图。
  - 提示文字："今天还没有训练记录，点击「添加动作」开始记录吧"。

- **司马黑互动:**
  - 页面顶部显示司马黑（encourage 状态）+ 对话气泡。
  - 有训练记录时："今天练了 N 个动作，不错嘛！"
  - 无训练记录时："今天打算练什么？司马黑等着呢。"

- **数据结构:**

| 类型 | 字段 | 说明 |
| :--- | :--- | :--- |
| **TrainingRecord** | id, dateStr, actions[], note, createdAt, updatedAt | 每日训练记录 |
| **ExerciseAction** | id, name, description, imageUrl, type, sets[], duration | 单个训练动作 |
| **ExerciseSet** | setNumber, value, weight | 单组训练数据 |
| **StarredAction** | id, name, description, imageUrl, type, defaultSets, defaultDuration, starredAt | 收藏的常用动作 |

- **localStorage Keys:**

| Key | 说明 |
| :--- | :--- |
| `litch-check-training-records` | 所有训练记录数组 |
| `litch-check-starred-actions` | 所有收藏的常用动作数组 |
=======
  - 在底部导航栏新增独立的"锻炼"模块/Tab，用于详细记录和管理用户的训练活动。

- **训练记录:**
  - 用户可以按天记录训练，每条记录包含多个训练动作。
  - 每个动作可以记录**动作名称**、**组数/时长**、**备注**和**日期**。

- **Star 标记 (常用动作库):**
  - 用户可以 Star 某个训练动作，将其标记为"长期固定训练"。
  - Star 过的动作会优先显示在动作选择列表中，形成一个"常用动作库"，方便快速添加。

- **图片关联:**
  - 每个训练动作都关联一张**司马黑形象的锻炼图**，风格与应用整体保持一致。
  - V1.0 版本图片存储在本地 `client/public/exercise-images` 目录下。
  - 预置了 9 个训练动作及其图片：壶铃摇摆、深蹲、哑铃推举、转呼啦圈、椭圆仪、散步、泡沫滚轴放松、炮筒划船、太极拳。

- **导出卡片:**
  - 每个训练记录都可以导出一张精美的图片卡片。
  - 卡片为 3:4 竖版，上方是 1:1 的训练图片，下方是动作名称、简短说明、训练数据和日期。

- **训练图鉴:**
  - Star 过的训练项目会汇集在"图鉴"页面。
  - 用户可以按**打卡次数**或**时间轴**排序查看，回顾自己的训练历史和偏好。
>>>>>>> 43392b5a69e73bcf41583f0bbb29776fdca8dce0

## 4. 设计风格

- **孟菲斯设计 (Memphis Design):**
  - **主色调:** 采用柔和的桃色 (#FFB5A7) 作为背景主色调。
  - **装饰元素:** 页面中会随机浮动一些简单的几何图形（如圆形、三角形、波浪线），增加页面的趣味性和活力。
  - **周报卡片:** 采用渐变背景（#FFF5F0 → white → #F0E6FF），搭配孟菲斯风格的圆形、三角形、菱形装饰。
  - **锻炼模块:** 延续桃色系（#FFB7B2），训练卡片左侧珊瑚色强调条，Star 使用金色（#F0D56E），导出卡片带孟菲斯几何装饰。

## 5. 项目结构

```
litchs-check/
├── client/src/
│   ├── App.tsx                          # 路由 + 底部导航栏（4 个 tab）
│   ├── pages/
│   │   ├── DailyCheckin.tsx             # 每日打卡页面
│   │   ├── CalendarView.tsx             # 月日历视图
│   │   ├── WeeklyReviews.tsx            # 历史周报列表
│   │   ├── WeeklyReviewDetail.tsx       # 周报详情 + 导出
<<<<<<< HEAD
│   │   └── Exercise.tsx                 # 🆕 锻炼训练记录页面
│   ├── components/
│   │   ├── MemphisBackground.tsx        # 孟菲斯背景装饰
│   │   ├── WeeklyReviewCard.tsx         # 周报结算卡片组件
│   │   └── WeeklyReviewReminder.tsx     # 周一弹窗提醒
│   └── lib/
│       ├── imageToBase64.ts             # 图片转 base64 工具
│       └── exerciseStorage.ts           # 🆕 锻炼模块 localStorage 存储层
=======
│   │   ├── ExerciseTracker.tsx          # 锻炼模块主页面 (v3.0)
│   │   └── ExerciseCollection.tsx       # 训练图鉴页面 (v3.0)
│   └── components/
│       ├── MemphisBackground.tsx        # 孟菲斯背景装饰
│       ├── WeeklyReviewCard.tsx         # 周报结算卡片组件
│       └── WeeklyReviewReminder.tsx     # 周一弹窗提醒
>>>>>>> 43392b5a69e73bcf41583f0bbb29776fdca8dce0
├── server/
│   ├── routers.ts                       # tRPC API 路由
│   ├── db.ts                            # 数据库查询函数
│   ├── exerciseDb.ts                    # 锻炼模块数据库操作 (v3.0)
│   ├── weeklyReview.test.ts             # 周报 Vitest 测试
│   ├── checkin.test.ts                  # 打卡 Vitest 测试
│   └── exercise.test.ts                 # 锻炼模块 Vitest 测试 (v3.0)
├── shared/
│   ├── tasks.ts                         # 5 个任务类型定义
│   ├── catImages.ts                     # 猫图片 CDN URL
│   ├── weeklyReview.ts                  # 周报类型 + 猫评语逻辑
<<<<<<< HEAD
│   └── exercise.ts                      # 🆕 锻炼模块类型定义 + 预置动作
=======
│   └── exercises.ts                     # 锻炼模块类型定义和预置数据 (v3.0)
>>>>>>> 43392b5a69e73bcf41583f0bbb29776fdca8dce0
└── drizzle/
    └── schema.ts                        # 数据库表定义
```

## 6. API 端点

### 打卡相关

| 端点 | 方法 | 说明 |
| :--- | :--- | :--- |
| `trpc.checkin.getByDate` | Query | 获取指定日期的打卡数据 |
| `trpc.checkin.toggle` | Mutation | 切换任务完成状态 |
| `trpc.checkin.updateNote` | Mutation | 更新任务备注 |
| `trpc.checkin.deleteNote` | Mutation | 删除任务备注 |
| `trpc.checkin.monthlySummary` | Query | 获取月度汇总 |
| `trpc.checkin.streak` | Query | 获取连胜天数 |

### 周报相关 (v2.0 新增)

| 端点 | 方法 | 说明 |
| :--- | :--- | :--- |
| `trpc.weeklyReview.get` | Query | 获取指定周的结算数据（输入: weekStart "YYYY-MM-DD"） |
| `trpc.weeklyReview.list` | Query | 获取所有历史周报列表 |

### 日记相关

| 端点 | 方法 | 说明 |
| :--- | :--- | :--- |
| `trpc.dailyLog.get` | Query | 获取指定日期的日记 |
| `trpc.dailyLog.save` | Mutation | 保存/更新日记 |

### Root 批语相关 (v2.3 新增)

| 端点 | 方法 | 说明 |
| :--- | :--- | :--- |
| `trpc.gptComment.get` | Query | 获取指定日期的 Root 批语 |
| `trpc.gptComment.save` | Mutation | 保存/更新 Root 批语 |

<<<<<<< HEAD
### 锻炼模块 (v3.0 新增)

> **注意:** 锻炼模块使用 `localStorage` 本地存储，无 API 端点。所有数据操作通过 `client/src/lib/exerciseStorage.ts` 进行。
=======
### 锻炼模块相关 (v3.0 新增)

| 端点 | 方法 | 说明 |
| :--- | :--- | :--- |
| `trpc.exercise.getByDate` | Query | 获取指定日期的训练记录 |
| `trpc.exercise.add` | Mutation | 添加一条训练记录 |
| `trpc.exercise.update` | Mutation | 更新一条训练记录 |
| `trpc.exercise.delete` | Mutation | 删除一条训练记录 |
| `trpc.exercise.dates` | Query | 获取所有有训练记录的日期 |
| `trpc.exercise.stats` | Query | 获取各项训练的统计数据（次数、最近日期） |
| `trpc.exercise.starred` | Query | 获取所有收藏的训练动作 |
| `trpc.exercise.toggleStar` | Mutation | 切换训练动作的收藏状态 |
>>>>>>> 43392b5a69e73bcf41583f0bbb29776fdca8dce0

## 7. 数据库表

### checkins 表

| 字段 | 类型 | 说明 |
| :--- | :--- | :--- |
| id | INT (PK) | 自增主键 |
| dateStr | VARCHAR(10) | 日期 "YYYY-MM-DD" |
| taskId | INT | 任务 ID (1-5) |
| completed | BOOLEAN | 是否完成 |
| note | TEXT | 备注（可选） |
| createdAt | TIMESTAMP | 创建时间 |
| updatedAt | TIMESTAMP | 更新时间 |

### daily_logs 表

| 字段 | 类型 | 说明 |
| :--- | :--- | :--- |
| id | INT (PK) | 自增主键 |
| dateStr | VARCHAR(10) | 日期 "YYYY-MM-DD" |
| content | TEXT | 日记内容 |
| gptComment | TEXT | Root 批语内容（v2.3 新增） |
| createdAt | TIMESTAMP | 创建时间 |
| updatedAt | TIMESTAMP | 更新时间 |

<<<<<<< HEAD
> **注意:** 周报数据不需要额外的数据库表，直接从 `checkins` 表按日期范围聚合查询。锻炼模块数据存储在 `localStorage`，不使用数据库。
=======
### training_records 表 (v3.0 新增)

| 字段 | 类型 | 说明 |
| :--- | :--- | :--- |
| id | INT (PK) | 自增主键 |
| dateStr | VARCHAR(10) | 日期 "YYYY-MM-DD" |
| exerciseId | VARCHAR(64) | 训练动作 ID (slug) |
| sets | INT | 组数（可选） |
| duration | VARCHAR(32) | 时长（可选） |
| note | TEXT | 备注（可选） |
| createdAt | TIMESTAMP | 创建时间 |
| updatedAt | TIMESTAMP | 更新时间 |

### starred_exercises 表 (v3.0 新增)

| 字段 | 类型 | 说明 |
| :--- | :--- | :--- |
| id | INT (PK) | 自增主键 |
| exerciseId | VARCHAR(64) | 训练动作 ID (slug) |
| createdAt | TIMESTAMP | 收藏时间 |

> **注意:** 周报数据不需要额外的数据库表，直接从 `checkins` 表按日期范围聚合查询。
>>>>>>> 43392b5a69e73bcf41583f0bbb29776fdca8dce0

## 8. 资源与部署

- **吉祥物与图标:**
  - 4 张不同状态的猫吉祥物图片和 5 个手绘风格的任务图标均已上传至 S3 CDN。
  - 9 张锻炼模块的司马黑图片存放于 `client/public/exercise-images` 目录下。
  - CDN 域名: `files.manuscdn.com`

- **部署信息:**
  - **平台:** Manus webdev
  - **项目名:** `litchs-check`

- **代码仓库:**
  - **服务:** GitHub
  - **仓库地址 (SSH):** `git@github.com:NingYuleKK/litch-check.git`
  - **用户名:** NingYuleKK
  - **邮箱:** wangxinmi@gmail.com

## 9. 后续维护建议

- **数据备份:** 建议定期对数据库进行备份，以防数据丢失。
- **扩展新任务:** 未来如果需要增加新的任务类型，需要在 `shared/tasks.ts` 中添加新的任务定义，准备并上传新的手绘图标到 S3 CDN，并更新相关的阈值常量。
- **周报增强:** 可以考虑添加月报/季报功能，或者在周报中加入趋势对比（与上周相比）。
- **社交分享:** 导出的周报图片可以进一步集成微信/微博等社交平台的分享功能。
- **用户反馈:** 建议增加一个简单的用户反馈渠道，以便收集用户意见，持续改进产品。
- **锻炼模块扩展:**
  - **云端同步:** 未来可将 localStorage 数据迁移到 MySQL/TiDB，实现跨设备同步。
  - **司马黑动作图片:** 为每个训练动作制作专属的司马黑插图，替换当前的 placeholder。
  - **训练统计:** 添加训练频率、训练量趋势等数据可视化。
  - **训练计划:** 支持创建和管理训练计划模板。
  - **训练日历:** 在日历视图中展示训练记录。

## 10. 版本历史

| 版本 | 日期 | 主要变更 |
| :--- | :--- | :--- |
| v1.0 | 2026-02 | 基础打卡系统、成就系统、月日历视图 |
| v1.1 | 2026-02 | 手绘图标、日记功能、成就图标优化 |
| v1.2 | 2026-02 | 猫吉祥物集成（4 种状态） |
| v1.3 | 2026-02 | 猫对话气泡、连胜奖励消息、统计图表 |
| v1.4 | 2026-02 | 手绘任务图标上线 |
| v2.0 | 2026-02 | **周报结算系统**：自动生成周报、猫评语、历史列表、导出图片、周一弹窗提醒 |
| v2.1 | 2026-02-23 | **每日详情导出**：日历弹窗新增"导出图片"按鈕，将当天打卡记录导出为 PNG |
| v2.2 | 2026-02-23 | **导出修复 + 司马黑命名**：导出时将图片转为 base64 内嵌，彻底解决 CORS 问题；胖黑猫正式命名「司马黑」 |
| v2.3 | 2026-02-24 | **Root 批语功能**：每日结算弹窗新增 Root 批语区域，支持粘贴/编辑/保存 GPT 的点评，导出 PNG 图片包含批语内容 |
<<<<<<< HEAD
| v3.0 | 2026-03-02 | **锻炼模块**：新增独立锻炼训练记录页面，支持训练记录（组数/时长）、Star 标记常用动作库、图片展示、导出训练卡片（3:4 竖版 PNG），使用 localStorage 本地存储 |
=======
| v3.0 | 2026-03-02 | **锻炼模块**：新增独立锻炼 Tab，支持记录训练、Star 动作、关联图片、导出卡片、训练图鉴等功能 |
>>>>>>> 43392b5a69e73bcf41583f0bbb29776fdca8dce0
